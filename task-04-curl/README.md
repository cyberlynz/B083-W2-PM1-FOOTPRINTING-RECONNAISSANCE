# Task 04 — cURL HTTP Headers

## Objective
Read the HTTP response headers to inspect the server banner, status, cookies and redirects.

## Command

```bash
curl -I https://networkwalks.com
```

## Assignment Notes
The PDF explains that HTTP headers can reveal the web server, caching stack and hidden endpoints. The example references the WordPress REST API at `/wp-json/`.

## Evidence
Add your Kali terminal screenshot and complete command output after running the task.
